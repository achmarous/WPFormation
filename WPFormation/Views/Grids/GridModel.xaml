<UserControl x:Class="WPFormation.Views.GridModel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFormation.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition />
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>

        <!-- Menu Bar -->
        <Rectangle Grid.Row="0" Grid.Column="0"  Fill="red"/>

        <!-- Content Bar -->
        <Rectangle Grid.Row="1" Grid.Column="1"  Fill="Blue"/>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20*"/>
                <ColumnDefinition Width="60*"/>
                <ColumnDefinition Width="20*"/>
            </Grid.ColumnDefinitions>

            <Rectangle Fill="LightGray" />
            <Rectangle Grid.Column="1" Fill="Gray" />
            <Rectangle Grid.Column="2" Fill="DarkGray" />
        </Grid>

        <!-- Footer -->
        <Rectangle Grid.Row="2" Grid.Column="0"  Fill="Green"/>
    </Grid>
</UserControl>
